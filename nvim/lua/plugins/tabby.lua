-- tabby ml plugin (stubbed)
-- tab-accept keybind (<C-l>) historically clashed with tab spacing behavior.
-- Kept as a placeholder; planned replacement: on-demand local LLM autocomplete
-- via oMLX (gemma4 / qwen3 MoE) once that feature is implemented.

return {
	"TabbyML/vim-tabby",
	lazy = true,
	enabled = false,
}
